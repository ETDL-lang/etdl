[package]
name = "etdl-tree-core"
version = "0.3.1"
edition.workspace = true
license.workspace = true
repository.workspace = true
readme = "README.md"
description = "ETDL Generic Tree Event Supplement 1.0: a domain-neutral tree-of-events structural model (nodes, gates, validation, traversal, serialization). No dependency on reliability or probability crates — reliability, safety, security, and future domains consume this, not the reverse."
keywords = ["etdl", "tree", "graph", "event", "builtin"]
categories = ["data-structures", "science"]
homepage = "https://github.com/ETDL-lang/etdl"

[dependencies]
serde.workspace = true
serde_json.workspace = true
thiserror.workspace = true
