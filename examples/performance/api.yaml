asyncapi: "3.0.0"
info:
  title: "Order Fulfillment API (Performance Demo)"
  version: "1.0.0"
channels:
  orders:
    address: orders
    messages:
      OrderPlaced:
        payload:
          type: object
components:
  messages:
    OrderPlaced:
      payload:
        type: object
        properties:
          items:
            type: array
            items:
              type: object
              properties:
                qty:
                  type: integer
