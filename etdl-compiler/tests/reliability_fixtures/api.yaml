asyncapi: "3.0.0"
info:
  title: api
  version: "1.0.0"
channels: {}
components:
  messages:
    Event:
      name: Event
      payload:
        type: object
        properties:
          ok: { type: boolean }
