# CLI Reference

Moved: the maintained CLI reference is [docs/CLI.md](../CLI.md) — commands
(`compile`, `validate`, `analyze`, `reliability`, `library`, `discover`,
`supplement`, `capabilities`, `conformance`, `version`), exit codes, `--json`
output shapes, and diagnostics.

This file is kept as a redirect because external links may still point here.
